import styled from 'styled-components';

export const Container = styled.div`
  display: grid;
  place-content: center;
  padding: 1rem;
  border-radius: 5px;
  box-shadow: var(--s-shadow);
  background-color: var(--s-container);
`;
